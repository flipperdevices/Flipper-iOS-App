import SwiftUI

struct AppReportBanner: View {
    var body: some View {
        Banner(
            image: "Done",
            title: "Success",
            description: "App report has been sent"
        )
    }
}
