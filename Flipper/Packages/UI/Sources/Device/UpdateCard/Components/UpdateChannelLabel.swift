import SwiftUI

extension DeviceUpdateCard {
    struct UpdateChannelLabel: View {
        var body: some View {
            Text("Update Channel")
                .font(.system(size: 14, weight: .medium))
                .foregroundColor(.black30)
        }
    }
}
